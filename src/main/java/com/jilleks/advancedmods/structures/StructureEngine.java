import java.util.Random;\n\npublic class StructureEngine {\n    public static void main(String[] args) {\n        StructureEngine engine = new StructureEngine();\n        engine.generateDungeon(10, 10);\n    }\n\n    public void generateDungeon(int width, int height) {\n        char[][] dungeon = new char[height][width];\n        Random rand = new Random();\n\n        // Fill dungeon with walls and spaces\n        for (int y = 0; y < height; y++) {\n            for (int x = 0; x < width; x++) {\n                if (x == 0 || x == width - 1 || y == 0 || y == height - 1) {\n                    dungeon[y][x] = '#'; // Wall\n                } else {\n                    dungeon[y][x] = rand.nextBoolean() ? ' ' : '.'; // Room or empty space\n                }\n            }\n        }\n\n        // Example of adding a simple room\n        int roomX = rand.nextInt(width - 2) + 1;\n        int roomY = rand.nextInt(height - 2) + 1;\n        dungeon[roomY][roomX] = 'R'; // Room\n\n        // Print dungeon\n        for (int y = 0; y < height; y++) {\n            for (int x = 0; x < width; x++) {\n                System.out.print(dungeon[y][x]);\n            }\n            System.out.println();\n        }\n    }\n}